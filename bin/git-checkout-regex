#!/bin/sh

if [ "$*" = "" ]
then
  echo "nothing to do"
  exit 0
fi

if [ "$1" = "--" ]
then
  git checkout $*
else
  branchesCount=`git branch | grep $1 -c`
  branches=`git branch | grep $1 | sed 's/[ *]//g'`
  
  if [ $branchesCount -eq 0 ]
  then
    echo "no branch found"
    exit 0
  fi
  if [ $branchesCount -ge 2 ]
  then
    echo "multiples branches found : \n$branches"
    exit 0
  fi

  git checkout $branches
fi

