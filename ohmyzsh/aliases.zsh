# Some useful aliases

alias resource="source ~/.zshrc"

alias gw="./gradlew --daemon"

alias git="LANGUAGE=en_US.UTF-8 git" # Toujours utiliser l'anglais pour Git

alias -g COLOR='| ack-grep --passthru'

alias jdk8='export JAVA_HOME=~/dev/jdk8'
alias jdk11='export JAVA_HOME=~/dev/jdk11'
jdk8 # by default, we use Java8

